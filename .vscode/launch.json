{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Open Current File",  // <-- แนะนำให้แก้ชื่อตรงนี้ด้วย จะได้ไม่งง
            "file": "${file}"             // <-- จุดสำคัญที่ต้องแก้ครับ
        }
    ]
}